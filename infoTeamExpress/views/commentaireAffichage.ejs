<!DOCTYPE html>
<html>
<% include header %>
    <body>

        <div>

            <form action="/commentaire/creation" method="post">
                <div class="input" id="crea">
                    <label>commentaire</label> :
                    <input type="text" name="commentaire" required />
                    <input type="submit" value="Envoyer" />
                </div>

            </form>

            <ul>
                <% for (let i = 0; i < tabComN.length; i++) {%>
                    <li class="list-group-item">Nom:
                        <%= tabComN[i] %>
                            <br> message:
                            <%= tabComD[i]%>
                            <%= tabComC[i] %>
                                <form action="/commentaire/supplink/:<%= tabComI[i]%>" method="post">
                                    <button type="submit" class="btn btn-danger btn-sm">supprimer</button>
                                    <button type="button" onclick="toggleForm(<%= i %>)" class="btn btn-primary btn-sm">Modifier</button>
                                </form>

                                <div id="edit_<%= i %>" style="display:none;">
                                    <form action="/commentaire/modiflink/:<%= tabComI[i]%>" method="post">

                                        <input type="text" name="prenom" value="<%= tabComN[i] %>" hidden />
                                        <input type="text" name="commentaire" value="<%= tabComC[i] %>" required/>
                                        <button type="submit" class="btn btn-success btn-sm">modifier</button>
                                    </form>
                                </div>
                    </li>

                    </li>
                    <% } %>

            </ul>


        </div>
    </body>

    <% include footer %>

</html>